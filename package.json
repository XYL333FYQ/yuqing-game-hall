{
  "name": "yuqing-game-hall",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "packageManager": "pnpm@11.24.0",
  "scripts": {
    "games:catalog": "node scripts/generate-game-catalog.mjs",
    "build:fruit-party": "node scripts/build-fruit-party.mjs",
    "games:build": "node scripts/build-games.mjs",
    "prepare:platform": "npm run games:catalog && node scripts/copy-mediapipe-assets.mjs && node scripts/copy-legal-assets.mjs && node scripts/check-game-assets.mjs && npm run check:boundaries",
    "prepare:games": "npm run games:build && npm run prepare:platform",
    "prepare-assets": "npm run prepare:platform",
    "check:games": "node scripts/check-game-assets.mjs",
    "check:boundaries": "node scripts/check-project-boundaries.mjs",
    "check:encoding": "node scripts/check-text-encoding.mjs",
    "check:platform": "npm run games:catalog && npm run check:boundaries && npm run check:encoding && tsc --noEmit",
    "predev": "npm run prepare:platform",
    "dev": "vite --host 0.0.0.0",
    "dev:launcher": "vite --host 127.0.0.1",
    "prebuild": "npm run prepare:games",
    "build": "npm run check:platform && vite build && node scripts/prune-cloudflare-output.mjs && node scripts/check-project-boundaries.mjs --dist && node scripts/check-deploy-limits.mjs",
    "build:all": "npm run build && npm run server:fruit:build",
    "check": "npm run check:platform && npm run check:games:types && npm run server:fruit:check && npm run server:sanctuary:check && npm run server:webrtc:check",
    "check:games:types": "tsc -p tsconfig.games.json --noEmit",
    "pretest": "npm run games:catalog",
    "test": "npm run check:games:types && vitest run",
    "test:all": "npm run test && npm run server:fruit:test && npm run server:sanctuary:test && npm run server:config:test",
    "preview": "vite preview --host 0.0.0.0",
    "preview:cloudflare": "npm run build && wrangler pages dev dist",
    "deploy:cloudflare": "npm run build && wrangler pages deploy dist --project-name yuqing-game-hall",
    "server:fruit:build": "pnpm --dir server-games/fruit-party build",
    "server:fruit:check": "pnpm --dir server-games/fruit-party check",
    "server:fruit:dev": "pnpm --dir server-games/fruit-party dev",
    "server:fruit:start": "pnpm --dir server-games/fruit-party start",
    "server:fruit:test": "pnpm --dir server-games/fruit-party test",
    "server:sanctuary:check": "pnpm --dir server-games/sanctuarys-end check",
    "server:sanctuary:start": "pnpm --dir server-games/sanctuarys-end start",
    "server:sanctuary:test": "pnpm --dir server-games/sanctuarys-end test",
    "server:webrtc:check": "pnpm --dir server-games/webrtc-gateway check",
    "server:webrtc:start": "pnpm --dir server-games/webrtc-gateway start",
    "server:webrtc:test": "pnpm --dir server-games/webrtc-gateway test",
    "server:webrtc:smoke": "pnpm --dir server-games/webrtc-gateway test:smoke",
    "server:turn:check": "node server-games/scripts/check-turn.mjs",
    "server:env:generate": "node server-games/scripts/generate-env.mjs",
    "server:pages:sync": "node server-games/scripts/sync-pages-config.mjs",
    "server:config:test": "node --test --test-force-exit server-games/scripts/generate-env.test.mjs server-games/scripts/sync-pages-config.test.mjs"
  },
  "dependencies": {
    "@mediapipe/tasks-vision": "^1.0.1",
    "perfect-freehand": "1.2.3"
  },
  "devDependencies": {
    "@types/node": "^26.2.0",
    "typescript": "^7.0.2",
    "vite": "^8.2.1",
    "vitest": "4.1.11",
    "wrangler": "4.127.0"
  }
}
