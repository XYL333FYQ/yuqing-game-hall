{
  "name": "sanctuarys-end-dev",
  "version": "1.0.0",
  "private": true,
  "description": "Dev tooling (type-checking, lint/format, tests) for Sanctuary's End. The game itself ships as plain static files — nothing here is bundled, built, or served to players.",
  "scripts": {
    "typecheck": "tsc -p jsconfig.json --noEmit",
    "lint": "biome lint .",
    "format": "biome format --write .",
    "format:check": "biome format .",
    "test": "node --test \"tests/**/*.test.js\"",
    "check": "npm run typecheck && npm run lint && npm run format:check && npm test"
  },
  "devDependencies": {
    "@biomejs/biome": "1.9.4",
    "typescript": "5.6.3"
  }
}
