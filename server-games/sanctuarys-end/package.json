{
  "name": "@yuqing/sanctuary-relay",
  "private": true,
  "version": "1.0.0",
  "description": "Sanctuary's End presence and chat WebSocket relay",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "check": "node --check server.js && node --check tests/relay.test.mjs",
    "start": "node server.js",
    "test": "node --test --test-force-exit tests/relay.test.mjs"
  },
  "dependencies": {
    "ws": "8.21.0"
  }
}
