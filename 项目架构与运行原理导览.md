# 雨晴游戏厅：项目架构与运行原理导览

这份文档面向刚开始接触 Web 项目的读者。

目标不是只说明“怎么启动项目”，而是解释：

- 用户打开网站后发生了什么
- 浏览器、服务器和 Cloudflare 分别负责什么
- 目录和文件在什么时候被使用
- 果切游戏、第三方游戏、联机房间和视觉实验室如何运行
- 项目中哪些地方耦合较深
- 以后学习哪些知识最有帮助

## 1. 先用一句话理解这个项目

**雨晴游戏厅是一个网页游戏大厅。**

它把几种不同来源、不同运行方式的游戏放到同一个入口中：

- 果切派对：项目自己维护的原生 TypeScript 游戏
- 静态第三方游戏：作为网页文件放在 `public/games/`，通过 iframe 打开
- 需要常驻服务器的游戏：只提供介绍和外部在线地址
- 许可受限的项目：只保留说明，不复制运行代码
- 视觉实验室：使用浏览器摄像头、Web Worker、MediaPipe 和 WASM 做视觉识别

## 2. 系统整体架构

```text
用户浏览器
│
├─ 请求雨晴游戏厅网页
│  ├─ 下载 HTML
│  ├─ 下载 CSS
│  └─ 下载 Vite 构建后的 JavaScript
│
├─ 大厅页面
│  ├─ 首页
│  ├─ 游戏介绍页
│  ├─ 通用游戏播放页
│  └─ 视觉实验室
│
├─ 果切派对
│  ├─ 浏览器运行游戏规则
│  ├─ Canvas 绘制画面
│  ├─ Pointer 事件提供刀刃轨迹
│  ├─ 浏览器播放音乐和音效
│  └─ localStorage 保存部分本地设置
│
├─ 第三方静态游戏
│  └─ iframe 加载 /games/<game-id>/ 中的网页
│
├─ 好友联机
│  ├─ HTTP 创建或加入房间
│  ├─ WebSocket 保持实时连接
│  └─ 服务器同步比赛状态
│
└─ 视觉实验室
   ├─ 浏览器请求摄像头
   ├─ Web Worker 在后台线程运行视觉模型
   ├─ WASM 提供高性能模型运行时
   └─ 页面显示手部或人体识别结果

Cloudflare
│
├─ 静态文件托管：dist/
├─ Worker：worker/index.ts
├─ API：/api/health、/api/rooms 等
└─ Durable Object：GameRoom，保存每个联机房间的状态
```

## 3. 浏览器、服务器和 Cloudflare 的职责

### 浏览器负责什么

浏览器负责：

- 显示页面
- 执行 JavaScript
- 运行果切游戏循环
- 绘制 Canvas
- 接收鼠标、键盘和指针输入
- 播放音乐和音效
- 保存本地设置和最高分
- 请求摄像头
- 执行视觉模型
- 运行 iframe 中的第三方游戏
- 与 WebSocket 服务器保持连接

因此，用户点击“开始游戏”时，通常不需要访问服务器。

### 服务器负责什么

服务器负责：

- 返回网页文件
- 创建房间
- 加入房间
- 验证玩家凭据
- 接受 WebSocket 连接
- 保存玩家和比赛状态
- 验证切水果动作
- 判断比赛结果
- 处理掉线、重连和房间过期

服务器不负责每一帧绘制果切画面。每个玩家自己的浏览器负责显示本地画面。

### Cloudflare 负责什么

项目配置在 `wrangler.jsonc` 中，目标部署平台是 Cloudflare。

Cloudflare 在这个项目中提供：

1. 静态文件托管
2. Cloudflare Worker 后端
3. Durable Object 房间对象
4. Durable Object 自带的持久化存储

项目没有单独部署 MySQL、PostgreSQL 或 MongoDB。

更准确地说：

> 项目没有传统独立数据库服务，但联机房间使用了 Cloudflare Durable Object 的持久化存储。

## 4. 技术栈

### TypeScript

TypeScript 是带有类型检查的 JavaScript。

项目自有源码主要使用 TypeScript：

```text
src/
worker/
tests/
```

它可以提前发现很多错误，例如把字符串传给本应接收数字的函数。

替代方案：JavaScript、Flow。当前使用 TypeScript 是行业中很常见的选择。

### Vite

Vite 是前端开发服务器和构建工具，配置在 `vite.config.ts`。

开发时它：

- 启动本地服务器
- 读取 `index.html`
- 加载 `src/main.ts`
- 处理模块 import
- 提供快速热更新

构建时它：

- 编译 TypeScript
- 打包 JavaScript
- 处理 CSS
- 生成发布资源
- 将 `public/` 原样复制到 `dist/`

替代方案包括 Webpack、Rollup、Parcel 和 esbuild。

### pnpm

`package.json` 声明项目使用 `pnpm@11.24.0`。

pnpm 负责安装依赖、管理 `node_modules`，并配合 `pnpm-lock.yaml` 固定实际版本。

锁文件不是业务代码，它记录：

- 精确安装版本
- 依赖之间的关系
- 下载来源
- 间接依赖

### Vitest

Vitest 是测试工具，配置在 `vitest.config.ts`。

测试位于 `tests/`，主要验证：

- 水果生成
- 刀刃碰撞
- 联机比赛规则
- 房间码
- 音频状态
- 指针输入
- 游戏目录

### Canvas

Canvas 是浏览器提供的绘图区域。

果切游戏使用 Canvas 2D 绘制水果、刀光、粒子、果汁和 HUD。

每一帧大致是：

```text
更新游戏状态
↓
计算水果位置
↓
判断碰撞
↓
修改分数和生命
↓
清空并重绘 Canvas
```

替代方案包括 DOM、SVG、WebGL 或 Phaser/PixiJS 等游戏引擎。

### iframe

iframe 可以把一个网页嵌入另一个网页。

项目在 `src/pages/library-play.ts` 中使用 iframe 加载 `public/games/` 下的第三方游戏。

使用 iframe 的原因是：第三方游戏已经是完整网页，拥有自己的 HTML、CSS、JavaScript、纹理和音频。iframe 可以减少它们对大厅页面的污染。

替代方案：直接 import、页面跳转、Web Component、微前端或独立部署。

当前项目按游戏性质选择方式：

```text
果切派对       → 原生模块
静态第三方游戏 → iframe
需要独立服务   → 外部链接
许可受限项目   → 不提供运行入口
```

### WebSocket

WebSocket 是一种保持连接的双向通信协议。

普通 HTTP 通常是：

```text
浏览器请求一次
服务器响应一次
```

WebSocket 是：

```text
浏览器连接服务器
连接保持打开
浏览器和服务器都可以主动发送消息
```

果切联机客户端代码在 `games/fruit-party/network/MultiplayerClient.ts`，平台入口 `worker/index.ts` 只转出果切 Worker 实现；服务器代码位于 `games/fruit-party/server/worker.ts`。

### Web Worker

Web Worker 是浏览器中的后台线程。

视觉模型计算比较重，如果放在主线程，页面可能卡顿。因此项目把视觉识别放到：

```text
src/vision/workers/
```

主线程负责页面和绘制，Worker 负责模型计算，双方通过消息通信。

### WASM

WASM 是 WebAssembly，一种可以在浏览器高效执行的代码格式。

项目中，MediaPipe 的 WASM 运行时被复制到：

```text
public/wasm/
```

需要区分：

```text
.task 文件 → 模型数据
.wasm 文件 → 模型运行时
Worker     → 后台线程
VisionController → 摄像头和页面协调
```

## 5. 目录结构

### `src/`

项目自己维护的前端源码。

Vite 会读取它，编译成浏览器可以执行的 JavaScript。浏览器部署后通常不会直接读取 `src/*.ts`。

### `src/main.ts`

前端入口：

```text
找到 #app
↓
调用 startPortal()
```

### `src/portal/`

大厅路由和页面切换协调。

项目没有使用 React Router 或 Vue Router，而是自己根据 `window.location.pathname` 匹配路由。

### `src/pages/`

普通大厅页面：

```text
home.ts
library-game.ts
library-play.ts
vision-lab.ts
```

负责生成页面 HTML、绑定交互和清理事件。

### `src/platform/`

大厅共享能力：

```text
device.ts              判断设备是否适合玩游戏
navigation.ts          站内导航
shell.ts               页头、页脚、公共 UI
game-manifest.ts       Manifest 类型
game-catalog.ts        游戏目录读取
通用 iframe 播放器 读取 Manifest 入口
```

### `games/fruit-party/`

果切派对完整客户端实现：

```text
FruitNinjaEngine.ts       游戏主循环和总协调
systems/FruitSystem.ts    水果生成和移动
systems/SliceSystem.ts    刀刃碰撞
systems/ArcadeSystem.ts   街机模式
renderers/                游戏画面和 HUD
effects/                  果汁、粒子、飞溅
pages/                    单人、街机、联机和房间页面
```

### `games/fruit-party/network/` 与 `server/`

果切的 HTTP 请求、WebSocket 客户端、房间消息、比赛规则和 Durable Object 都归果切目录维护。`worker/index.ts` 通过构建生成的注册文件转出已安装游戏后端；平台本身不再提供果切专用的 network/shared 模块。

### `src/vision/`

摄像头、视觉控制器、滤波器、稳定器和 Worker。

### `worker/`

Cloudflare 后端入口，目前主要集中在 `worker/index.ts`。

它负责 HTTP API、房间创建、WebSocket、比赛状态机、持久化和定时器。

### `public/`

不经过 Vite 打包、按原路径发布的资源：

```text
public/games/     第三方网页游戏
public/art/       图片
public/audio/     音频
public/fonts/     字体
public/models/    视觉模型
public/wasm/      WASM 文件
public/legal/     许可证和来源说明
```

### `dist/`

构建产物，不是源码，也不应手动修改。

它由以下内容组成：

```text
src 编译后的文件
+
public 原样复制的文件
```

### `node_modules/`

本地依赖，只在开发、构建、测试和打包时使用，浏览器不会直接读取它。

### `game-sources/`

第三方上游源码记录，用于追溯、审查、许可证核对和后续同步。

### `server-games/`

需要独立服务器的游戏资料和配置边界。

## 6. 程序从 URL 到页面的运行过程

```text
用户输入网址
↓
DNS 把域名转换成服务器地址
↓
浏览器请求 HTML
↓
Cloudflare 返回 index.html
↓
浏览器加载 CSS 和 JavaScript
↓
执行 main.ts 编译后的代码
↓
startPortal(app)
↓
读取当前 URL
↓
匹配路由
↓
动态加载页面模块
↓
页面生成 HTML
↓
浏览器显示页面
```

浏览器不会直接执行 TypeScript。实际流程是：

```text
TypeScript
↓ Vite 构建
JavaScript
↓ 浏览器下载
浏览器执行
```

## 7. 果切单人游戏的数据流

用户点击“开始游戏”后：

```text
点击按钮
↓
fruit-play.ts 的 clickHandler
↓
调用 start()
↓
audio.unlock()
↓
隐藏开始遮罩
↓
engine.beginGame()
↓
设置初始状态和倒计时
↓
requestAnimationFrame(loop)
↓
FruitSystem 更新水果
↓
PointerBladeController 提供刀刃轨迹
↓
SliceSystem 判断碰撞
↓
GameRenderer 绘制游戏
↓
HUDRenderer 绘制分数
```

这个过程通常不访问服务器。

鼠标移动时：

```text
指针事件
↓
PointerBladeController
↓
转换为 Canvas 坐标
↓
engine.updateBlade()
↓
记录刀刃轨迹
↓
SliceSystem 判断是否穿过水果
```

## 8. 好友联机的数据流

### 创建房间

```text
用户输入昵称和模式
↓
fruit-online.ts
↓
校验昵称
↓
roomRequest("/api/rooms", ...)
↓
HTTP POST
↓
Cloudflare Worker
↓
生成房间码
↓
GameRoom.create()
↓
保存房间
↓
返回房间码、玩家 ID、token
↓
浏览器保存到 sessionStorage
↓
跳转 /room/ABC234
```

### 建立 WebSocket

```text
room.ts
↓
MultiplayerClient.connect()
↓
new WebSocket(...)
↓
Worker upgrade()
↓
GameRoom.upgrade()
↓
验证 playerId 和 token
↓
接受 WebSocket
```

### 发送切割动作

客户端不会发送完整画面，而是发送切割记录：

```text
水果 ID
切割时间
起点
终点
持续时间
视口尺寸
```

服务器收到后：

```text
检查消息格式
↓
检查水果是否存在
↓
检查是否已经处理过
↓
根据同一个 seed 重建水果时间表
↓
检查切割时间
↓
检查刀刃线段是否真的穿过水果
↓
增加分数或处理炸弹
↓
广播房间快照
```

这里的设计是：

> 浏览器负责灵敏的操作感，服务器负责最终裁判。

如果完全相信客户端，玩家可以伪造“我切了很多水果”的消息。

## 9. 架构依赖关系

```text
index.html
   ↓
src/main.ts
   ↓
portal/router.ts
   ├─ pages/home.ts
   ├─ pages/library-game.ts
   ├─ pages/library-play.ts
   └─ library-play.ts 统一 iframe 播放器
          ↓
       games/fruit-party/routes.ts
          ├─ fruit-details.ts
          ├─ fruit-play.ts
          ├─ fruit-arcade.ts
          ├─ fruit-online.ts
          └─ room.ts
                 ├─ FruitNinjaEngine
                 ├─ GameAudio
                 ├─ PointerBladeController
                 └─ MultiplayerClient
```

```text
各游戏目录中的 game.json
   ↓ 构建时扫描
generate-game-catalog.mjs
   ├─ src/generated/gameCatalog.ts（平台目录）
   └─ worker/generated-game-registry.ts（游戏后端注册）
        ↓
首页 / 游戏详情 / 通用播放页 / Worker API
```

### 低耦合的地方

- 第三方游戏和大厅玩法相互隔离
- 游戏资料通过 Manifest 管理
- 游戏自己的前后端规则和协议位于游戏目录
- 视觉实验室不会在首页自动加载模型
- 上游源码与正式发布目录分开
- 静态游戏和服务器游戏使用不同入口

### 耦合较深的地方

#### 果切页面和引擎

果切页面同时负责：

- 生成界面
- 初始化引擎
- 控制音频
- 处理暂停
- 绑定键盘和鼠标
- 处理结算
- 处理页面隐藏
- 处理联机客户端

#### `FruitNinjaEngine` 和浏览器

引擎同时知道：

- 游戏规则
- Canvas
- ResizeObserver
- requestAnimationFrame
- localStorage
- devicePixelRatio

理想上可以拆成：

```text
纯游戏规则
↓
浏览器适配层
↓
页面控制层
```

当前仍然可以正常运行，只是未来扩展的成本会提高。

#### Worker 后端文件

`worker/index.ts` 同时负责：

- HTTP 路由
- 房间创建
- WebSocket
- 比赛状态机
- 分数结算
- 房间过期
- Durable Object 存储

未来规则变复杂时，适合拆分。

## 10. 运行时、构建时和部署时

### 开发时

```text
pnpm dev
↓
predev
↓
prepare-assets
↓
生成游戏目录
↓
复制 MediaPipe WASM
↓
复制许可证
↓
检查游戏入口
↓
启动 Vite
```

### 构建时

```text
pnpm build
↓
生成游戏目录
↓
TypeScript 类型检查
↓
Vite 打包
↓
生成 dist
↓
检查静态资源数量和大小
↓
Wrangler 试打包 Worker
↓
检查 Worker 压缩体积
```

### 部署时

```text
pnpm deploy
↓
执行构建
↓
Wrangler 部署静态资源和 Worker
```

### 用户访问时

用户拿到的是：

```text
HTML
JavaScript
CSS
图片
音频
字体
模型
WASM
第三方网页游戏
```

用户不会直接拿到：

```text
src/
tests/
scripts/
node_modules/
game-sources/
```

## 11. 隐藏知识点

### 浏览器主线程

主线程负责页面、事件和动画。视觉模型放到 Worker，是为了避免模型计算阻塞页面。

### 浏览器时间和服务器时间不同

联机客户端会估算服务器时间偏移，否则双方倒计时可能不一致。

### 网络延迟

客户端发送切割事件到服务器需要时间，所以服务器验证时会允许一定的时间误差。

### localStorage 不等于数据库

它只是浏览器本地键值存储，不会自动同步到别的设备，也不是账号系统。

### 项目没有账号系统

联机玩家用房间码、玩家 ID 和 token 识别，适合临时好友房，不等于注册用户。

### 项目没有传统数据库服务器

房间数据保存在 Durable Object 存储中。

### 项目没有 SSR

页面主要由浏览器 JavaScript 生成，属于客户端渲染。

### 项目没有 ORM

没有 MySQL/PostgreSQL 表，也没有传统 ORM。房间对象直接保存结构化状态。

### 项目没有消息队列

WebSocket 消息直接进入房间对象。当前规模小，这样简单；大规模异步任务才需要队列。

### 缓存

浏览器和 CDN 可能缓存旧资源。Vite 生成的资源通常带哈希，但第三方静态游戏仍然需要谨慎处理版本更新。

### 第三方许可证

项目维护 `licenses/`、`public/legal/`、`SOURCE_AUDIT.md` 和 `THIRD_PARTY_NOTICES.md`，因为能运行不代表可以合法重新发布。

## 12. 如果重新设计

### 建议保留

- Manifest 作为游戏资料来源
- 独立静态游戏、外部服务和平台本身分开
- 前后端共享比赛规则
- 视觉模型使用 Worker
- 构建前检查资源和许可证

### 建议逐步改进

1. 把果切页面中的 UI、网络、音频和游戏控制逐步拆开
2. 让引擎更少依赖浏览器 API
3. 把 Worker 拆成路由、房间、规则和存储模块
4. 给 WebSocket 收到的数据增加完整的运行时校验
5. 给每次 WebSocket 连接增加连接编号，防止旧连接事件影响新连接

这些不是必须立即完成的重写，而是项目规模扩大时的演进方向。

## 13. AI 快速开发可能留下的痕迹

以下设计能快速完成项目，但以后可能增加维护成本：

- 页面文件同时承担模板、事件和业务流程
- 游戏引擎同时处理规则、Canvas、动画和本地存储
- Worker 文件集中处理很多职责
- 页面大量使用字符串模板生成 HTML
- 部分网络数据依赖 TypeScript 类型断言
- 浏览器 API 直接写在业务模块中，没有统一适配层

这些方案不一定错误。它们更像是“先让产品完整运行，再根据规模拆分”的方案。

## 14. 学习路线

### 基础知识

```text
HTML
CSS
JavaScript
TypeScript
URL
HTTP
JSON
模块 import/export
async/await
浏览器与服务器的区别
```

### 项目直接使用的知识

```text
Vite
pnpm
客户端路由
Canvas
requestAnimationFrame
localStorage
sessionStorage
iframe
WebSocket
Web Worker
WASM
MediaPipe
Cloudflare Worker
Durable Object
单元测试
```

### 进阶知识

```text
状态机
事件驱动架构
运行时协议校验
网络延迟补偿
服务端权威判定
缓存策略
CSP
iframe sandbox
CDN
数据库设计
日志和监控
自动化部署
```

### 暂时不用深入

```text
SSR
微前端
Kubernetes
消息队列集群
复杂 ORM
分布式事务
服务网格
数据库分片
```

## 15. 最后总结

这个项目可以分成四个核心部分：

```text
大厅前端
果切游戏
联机 Worker
静态第三方游戏和视觉资源
```

最重要的理解方式是：

> `src` 是项目源码，`public` 是直接发布的资源，`dist` 是构建结果，`worker` 是联机后端，浏览器负责大部分画面和游戏计算，Cloudflare 负责发布文件和保存联机房间。

“果切和界面耦合较深”具体指：果切页面同时控制界面、引擎、音频、浏览器事件和联机流程；果切引擎同时知道游戏规则、Canvas、动画循环和本地存储。

当前规模下它可以正常工作。以后如果加入更多自己维护的游戏，最值得优先做的是逐步分离这些职责，而不是立刻重写整个项目。

