# 开源底座源码审查记录

最后更新：2026-09-02。

## 1. 果切玩法底座

正式采用 `forinda/canvas-games` 的 Fruit Ninja 模块：

- 仓库：https://github.com/forinda/canvas-games
- 许可证：仓库包含正式 MIT 文件，本项目保留完整许可证。
- 已审源码：游戏循环、水果抛射、线段与圆碰撞、切半、粒子、炸弹、生命和 HUD。
- 本项目继续保留的部分：抛物线、水果波次、分半概念、粒子、炸弹、三条命、连击与本地最高分。
- 本项目重新实现的部分：平台页面、Pointer Events 鼠标控制、逐合并采样碰撞、视觉刀光、2.5D 绘制、确定性联机时间线和 Cloudflare 房间协议。

参考但不复制 `yappeizhen/frootninja`：其 WebRTC 房间和确定性随机数思路有参考价值，但根目录缺少完整独立 LICENSE，且摄像头推理和游戏刷新耦合，因此没有复制源码。

以下项目因玩法过简、旧模型或缺少正式许可证而未采用：

- `maze-srs/Augmented-Fruit-Ninja`
- `charliegerard/splat`
- `collidingScopes/fruit-ninja`

公开产品名使用“果切派对”，没有使用“水果忍者”的商标、图标或原作美术。

### 水果美术底图

- 采用 `googlefonts/noto-emoji` 中的西瓜、橙子、苹果、柠檬、猕猴桃、草莓和炸弹 SVG，许可证为 Apache-2.0。
- SVG 均保存为本地静态资源，不在运行时请求 GitHub 或第三方 CDN。
- 上游 SVG 只承担基础轮廓和配色；游戏内的放大尺度、体积光、动态阴影、切面、果籽、汁液贴花、刀光、炸弹引线火花和命中反馈由本项目统一绘制。炸弹生成前没有位置或时间提示。
- Noto 上游版权与许可声明保存在 `licenses/NOTO-EMOJI-APACHE-2.0.txt`，Apache-2.0 完整文本保存在 `licenses/GOOGLE-MEDIAPIPE-APACHE-2.0.txt`，登记见 `THIRD_PARTY_NOTICES.md`。

## 2. 鼠标刀刃

- 游戏不再接收任何视觉模型输出。
- 按住鼠标左键后才切割，松开后只保留瞄准点。
- 标准 `pointermove` 在所有支持浏览器中启用，并通过 `getCoalescedEvents()` 读取同一事件内的高频硬件采样。
- 每个未处理采样段都进入碰撞系统，避免一帧只检查最后一段而跳过水果。
- `perfect-freehand` 只从短时采样生成收尖、变宽的闭合刀光轮廓，碰撞仍使用原始线段。
- 自动浏览器扫刀测试已经确认鼠标采样进入引擎并能够切中水果；真实鼠标手感仍以玩家实际使用为最终验收。

## 3. 摄像头视觉底座

正式采用 `google-ai-edge/mediapipe-samples-web` 的 Worker 结构，Apache-2.0：

- Hand Landmarker 和 Holistic Landmarker 都在 Web Worker 中运行。
- 优先 GPU，初始化失败回退 CPU。
- 使用 Transferable `ImageBitmap`，同一 Worker 串行推理，不积压过期摄像头帧。
- 摄像头请求 640×360，推理输入缩放为 512×288，并显示真实相机 FPS、AI FPS、推理时间和管线延迟。
- 模型只由 `/vision-lab` 懒加载，不进入首页或果切运行路径。

保留 One Euro 自适应滤波和物理手身份匹配，用于实验室关键点显示及未来节奏较低的体感游戏。它不再被宣称能够达到高速果切所需的刀尖延迟。

已经删除：

- 旧 `@mediapipe/hands` Lite 包、约 2MB Lite 模型、旧 WASM 和独立 Worker。检测到真实手时它没有优于完整 Tasks 后端。
- `inspirit/jsfeat` 固定像素光流实验。合成方块测试无法代表真实手指，用户实测更差。
- 摄像头到游戏刀刃的 `VisionInputAdapter`。

## 4. 模型和 Cloudflare 文件限制

| 文件 | 实际大小 | Cloudflare 单文件 25 MiB |
|---|---:|---|
| `hand_landmarker.task` | 7,819,105 字节 | 通过 |
| `holistic_landmarker.task` | 13,683,609 字节 | 通过 |

WASM 从已安装的 `@mediapipe/tasks-vision` 复制到 `public/wasm/`。生产运行不访问第三方 CDN。构建脚本还会检查 `dist` 文件数量和每个文件大小。

## 5. 联机实现边界

- 使用 Cloudflare Worker、Workers Static Assets、Durable Objects 和原生 WebSocket，没有 WebRTC 视频。
- Durable Object 生成种子和开始时间；客户端本地模拟，服务器验证切割声明并维护权威比分。
- 本地双浏览器已经通过建房、加入、准备、权威比分同步、短线重连、认输和双方重赛流程。
- 当前属于私人休闲房间的基础防刷分，不宣称能抵抗修改客户端或专门攻击者。
- 没有账号、全球排行榜或长期玩家档案；昵称和本地最高分保存在浏览器，房间状态按时清理。

## 6. 仍需真实设备确认

Hand Landmarker 与 Holistic 已使用合成视频源确认本地模型、WASM、Worker、GPU 路径和关闭清理能够实际运行；该测试只验证启动链路，没有被当作真人挥手性能数据。

- Chrome/Edge 中不同刷新率鼠标的主观刀感。
- 两台不同网络电脑访问正式 Cloudflare 部署时的往返延迟。
- 体感实验室在用户真实双手、弱光、运动模糊和全身模式下的持续性能。

构建、单元测试和自动浏览器检查不能替代以上真实设备验收。

## 7. 街机模式与果汁喷溅

- 新增 90 秒本地街机时间线：无炸弹热身、三个由种子抽取且不重复的挑战、普通过渡和八刀巨型水果终局。该生成器与 `ScheduledFruit`、Durable Object 以及三种联机规则完全分离。
- 新喷溅模块参考 `NDevTK/Dynamic-Random` 的种子化轮廓、卫星滴、重力运动和对象池思路，以及 `oleksnd/ray.js` 的噪声湿斑和贝塞尔下淌思路；两者均为 MIT，完整许可已保存在 `licenses/`。
- 没有引入两套上游绘图库，也没有逐行复制其完整实现。本项目按水果尺寸、刀速和刀向重新实现为 TypeScript 数据结构，并限制为 12 个墙渍和 180 个动态液滴。
- 所有墙渍几何只在命中瞬间由种子生成一次；渲染循环不调用随机数。墙面层在屏幕震动变换之外，并使用归一化坐标，所以画面震动或尺寸变化不会让墙渍脱离背景。

## 8. 界面字体与视觉资源

- 标题、街机数字和短标签使用本地化的得意黑（Smiley Sans）WOFF2，来源为 `atelier-anchor/smiley-sans`，许可证为 SIL OFL 1.1。
- 字体只用于短标题和游戏展示数字；正文、规则说明和表单继续使用系统中文界面字体，避免窄斜体影响长文可读性。
- 网站运行时不向字体服务或第三方 CDN 发起请求。完整字体许可保存在 `licenses/SMILEY-SANS-OFL-1.1.txt`。
- 视觉重构保留现有水果 SVG 与浅色游戏厅／深色游戏舞台的基础，移除大面积紫色渐变、玻璃拟态、英文装饰标签和过度圆角卡片，改用暖纸色、鲜果橙、叶绿与街机海报式边框。

## 9. 阶段音乐与无尽随机性

- 背景音乐采用 OpenGameArt 的 `5 Chiptunes (Action)`，同一作者的五首曲目均为 CC0 且原页面声明可无缝循环。原始 WAV 转为本地 OGG Vorbis 后合计约 5.06 MiB，不使用运行时音乐 CDN。
- 音乐只在玩家点击开始或准备、浏览器允许播放后加载；倒计时、无尽难度层级、街机热身／事件／过渡／狂热／巨果和结算使用不同曲目或速度，并以约 720ms 交叉淡化。
- 静音同时控制背景音乐和程序化切割音效；暂停、页面隐藏、离开页面和销毁游戏实例会暂停或释放音乐元素。
- 无尽模式每局使用新的随机种子，首枚炸弹可落在约 5.2–14.0 秒的宽窗口。炸弹在自由弧线、横穿、中心外抛、边缘内收和近垂直五类轨迹间轮换，连续两枚避免重复同一类轨迹和相近起点。
- 后续炸弹间隔包含普通节奏、短促连压和随机空档；25 秒提升一次可见难度，水果数量、生成间隔和飞行速度同时变化。炸弹仍须通过完整相对轨迹间距校验，难度提升不靠制造不可解重叠。

## 10. 后期密集场景性能

- 不降低既定效果上限：仍保留最多 12 个墙渍、180 个动态果汁粒子，以及原有水果切半寿命、阴影、刀光和高清像素密度。
- 六类水果的左右切半主体和屏幕空间阴影在游戏开始前生成高分辨率本地缓存；运动、旋转、透明度与分离物理仍逐帧更新。
- 墙渍的三角函数坐标和 `Path2D` 只生成一次；粒子、墙渍、切半、击中特效、标签、刀迹和水果使用原数组压缩回收，避免密集切割时的垃圾回收尖峰。
- 同一刀段命中的多颗水果合并为一次切割音效，避免同一帧瞬间创建大量 Web Audio 节点；计分、碰撞、果汁和美术效果不合并。
- 最高分的状态仍即时更新到 HUD，但同步存储写入延后至结算、重开或离开页面；Canvas 调试属性约每 100ms 同步一次，不参与可见 HUD。

## 11. 游戏厅开源游戏整合

十个上游仓库保持原样归档在 `game-sources/upstream/`，不会从那里直接提供运行资源。实际进入源码和游戏流程后按运行结构分为三组：

- 可静态部署：Der Koloss CE、Sanctuary's End、LittleJS Arcade、PVP 的单机/本地模式、HexGL。清理后的副本位于 `public/games/`，最大单文件均低于 Cloudflare 25 MiB。
- 需常驻服务器：Suroi（Bun）、Scribble.rs（Go/WebSocket）、TOSIOS（Node/Colyseus）与 OpenFront（Node 权威服务）。静态站不能替代这些房间后端，自建说明位于 `server-games/`。
- 不复用：Kaetram Open 的自定义许可证明确禁止 AI 相关使用，本项目只保留名称、判断和源码链接。

静态移植使用共享 `yuqing-bridge.js` 翻译 DOM 文本并向游戏厅报告就绪，但不把翻译结果冒充上游原生完整中文版本。PVP 的 Canvas 位图字体只含 ASCII，因此在引擎打印函数中增加了只针对非 ASCII 文本的系统中文字体回退；英文 HUD 仍走原像素字形。

Der Koloss 的上游 `NOTICE.md` 明确指出商业歌曲 `beauty-of-annihilation.mp3` 未获再发布许可。游戏厅运行包没有复制该文件，音乐播放函数和资源档案入口也已去除引用。其他商标与致敬边界继续在详情页提示，技术可部署不等同于已获得商业品牌授权。

LittleJS Arcade 原目录中四个条目实际依赖第三方 itch.io iframe，容易被跨站策略、网络或扩展拦截并表现为永久加载。游戏厅运行包删除这些远程条目，只保留本地子游戏。HexGL 同时移除了外部统计脚本和远程 favicon，运行时不再为这两项访问第三方。

LittleJS 的 63 款本地子游戏共用本地汉化桥接层：DOM 菜单、教程、操作说明和常用 Canvas HUD 均在运行时转换为中文，实际键名（WASD、Shift、Enter 等）与角色专名保留原文。Chrome 逐页加载审查未发现长段英文菜单残留、脚本错误或 4xx 游戏资源。

Der Koloss 已继续覆盖房间、角色、辅助选项、操作设置与常见战斗提示；Sanctuary's End 已覆盖角色、帮助、设置、装备属性与部位、技能、符文、区域、怪物及主要商店界面。PVP 除 DOM 启动设置外，还在引擎文字出口统一翻译动态 Canvas HUD 与对局播报；玩家昵称、键位缩写、武器或世界专名会按可辨识性保留。以上三款均已用 Chrome 进入实际二级面板复查，不能据此宣称所有剧情和极低频分支都已完成全文翻译。

Der Koloss 上游自带的 27 个脚本校验器在当前归档提交与 Node 24 环境中为 22 个通过、5 个失败。`validate-game-invariants` 受到它作为嵌套目录继承主项目 ESM 类型的影响；其余四个校验器的源码模式与同一提交中的武器、模型或联机实现已经不一致。Chrome 实际启动、单人入场和第一波 HUD 已通过，但在这些上游校验器修复且商标素材替换前，不把该移植声明为可公开商用的最终版本。
